#ifndef PYMODE_H
#define PYMODE_H

#include "Models/System.h"

/// @brief runs the py mode (for the python GUI)
/// @param atmega the system to run
void run_py_mode(System* atmega);

#endif  // PYMODE_H